rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Restrict reads and writes under chambers/{chamberId}/ to users with matching chamber_id claims
    match /chambers/{chamberId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.token.chamber_id == chamberId;
    }
  }
}
